import React from 'react' 

const Workspace = () => {
        // add elements to workspace.js
        // put style in-line
        <div style = {
                {
                        width: "100px"
                }
        } ></div>

        // take out null
        return (null)
}

export default Workspace
